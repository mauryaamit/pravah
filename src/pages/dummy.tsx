// src/pages/dummy.tsx
// Dummy file to force Next.js to generate Pages Router manifests (e.g. pages-manifest.json).
// This prevents next-pwa build issues in App-Router-only projects.

export default function DummyPage() {
  return null;
}
